<project name="scuff" default="make-jar" xmlns:ivy="antlib:org.apache.ivy.ant">

	<target name="ivy-download" depends="init">
		<mkdir dir="lib" />
		<get src="http://repo2.maven.org/maven2/org/apache/ivy/ivy/${ivy.version}/ivy-${ivy.version}.jar" dest="${ivy.jar}" usetimestamp="true" />
	</target>

	<target name="scala-download" depends="init">
		<mkdir dir="lib" />
		<get src="http://repo2.maven.org/maven2/org/scala-lang/scala-library/${scala.version}/scala-library-${scala.version}.jar" dest="${scala-lib.jar}" usetimestamp="true" />
		<get src="http://repo2.maven.org/maven2/org/scala-lang/scala-compiler/${scala.version}/scala-compiler-${scala.version}.jar" dest="${scala-comp.jar}" usetimestamp="true" />
	</target>

	<target name="ivy-taskdef" depends="ivy-download">
		<path id="ivy.lib.path">
			<pathelement location="${ivy.jar}" />
		</path>
		<taskdef resource="org/apache/ivy/ant/antlib.xml" uri="antlib:org.apache.ivy.ant" classpathref="ivy.lib.path" />
	</target>

	<target name="scala-taskdef" depends="scala-download">
		<taskdef resource="scala/tools/ant/antlib.xml">
			<classpath>
				<pathelement location="${scala-lib.jar}" />
				<pathelement location="${scala-comp.jar}" />
				<pathelement location="${scala-refl.jar}" />
			</classpath>
		</taskdef>
	</target>

	<target name="resolve-deps" depends="ivy-taskdef" description="Resolve dependencies">
		<ivy:retrieve />
	</target>

	<target name="init" depends="clean">
		<property name="scala.major-minor" value="2.10" />
		<property name="scala.rev" value="2" />
		<property name="scala.version" value="${scala.major-minor}.${scala.rev}" />
		<property name="ivy.version" value="2.3.0" />
		<property name="scuff.version" value="1.2-SNAPSHOT" />
		<property name="ivy.jar" value="lib/ivy-${ivy.version}.jar" />
		<property name="scala-lib.jar" value="lib/scala-library-${scala.version}.jar" />
		<property name="scala-comp.jar" value="lib/scala-compiler-${scala.version}.jar" />
		<property name="scala-refl.jar" value="lib/scala-reflect-${scala.version}.jar" />
		<mkdir dir="build" />
	</target>

	<target name="clean">
		<delete dir="build" />
	</target>

	<target name="compile-main" depends="init, resolve-deps, scala-taskdef">
		<mkdir dir="${build.destdir}" />
		<scalac srcdir="src/main" destdir="${build.destdir}" unchecked="yes" addparams="${scalac.params}">
			<bootclasspath>
				<pathelement location="${scala-comp.jar}" />
				<pathelement location="${scala-lib.jar}" />
				<pathelement location="${scala-refl.jar}" />
			</bootclasspath>

			<classpath>
				<fileset dir="lib">
					<include name="**/*.jar" />
					<exclude name="scala-*" />
				</fileset>
			</classpath>
			<include name="scala/**/*.scala" />
			<!--include name="java/**/*.java" /-->
		</scalac>
	</target>

	<target name="compile-test" depends="init, resolve-deps, scala-taskdef">
		<property name="build.destdir" value="build/test" />
		<property name="scalac.params" value="-nowarn -optimise -language:implicitConversions" />
		<mkdir dir="build/test" />
		<antcall target="compile-main">
			<param name="build.destdir" value="${build.destdir}" />
			<param name="scalac.params" value="${scalac.params}" />
		</antcall>
		<scalac srcdir="src/test" destdir="${build.destdir}" addparams="${scalac.params}">
			<bootclasspath>
				<pathelement location="${scala-comp.jar}" />
				<pathelement location="${scala-lib.jar}" />
				<pathelement location="${scala-refl.jar}" />
			</bootclasspath>

			<classpath>
				<pathelement location="${build.destdir}" />
				<fileset dir="lib">
					<include name="**/*.jar" />
					<exclude name="scala-*" />
				</fileset>
			</classpath>
			<include name="scala/**/*.scala" />
			<!--include name="java/**/*.java" /-->
		</scalac>
	</target>

	<target name="run-junit" depends="compile-test" description="Run JUnit tests">
		<copy todir="build/test">
			<fileset dir="src/main/resources" />
			<fileset dir="src/test/resources" />
		</copy>
		<mkdir dir="build/junit" />
		<junit fork="yes" haltonfailure="true" haltonerror="true">
			<formatter type="xml" />
			<jvmarg value="-ea" />
			<jvmarg value="-XX:-UseSplitVerifier" />
			<classpath>
				<fileset dir="lib">
					<include name="**/*.jar" />
					<exclude name="ant-*" />
				</fileset>
				<pathelement path="build/test" />
				<pathelement location="${scala-lib.jar}" />
			</classpath>
			<batchtest todir="build/junit">
				<fileset dir="build/test">
					<include name="**/*Test*.class" />
					<exclude name="**/*$*.class" />
					<exclude name="**/*Abstract*.class" />
				</fileset>
			</batchtest>
		</junit>
	</target>

	<target name="make-jar" depends="init">
		<mkdir dir="build/main" />
		<antcall target="compile-main">
			<param name="build.destdir" value="build/main" />
			<param name="scalac.params" value="–unchecked -deprecation –explaintypes –Xdisable-assertions -feature -optimise -language:implicitConversions -Yinline-warnings" />
		</antcall>
		<tstamp />
		<manifest file="build/MANIFEST.MF">
			<attribute name="Built-By" value="${user.name}" />
			<section name="scuff">
				<attribute name="Implementation-Title" value="scuff" />
				<attribute name="Implementation-Version" value="${scuff.version} ${TODAY}" />
				<attribute name="Implementation-Vendor" value="nilskp" />
				<attribute name="Sealed" value="true" />
			</section>
		</manifest>
		<copy todir="build/main">
			<fileset dir="src/main/resources" />
		</copy>
		<jar basedir="build/main" destfile="build/${ant.project.name}-${scuff.version}-scala-${scala.major-minor}.jar" index="true" manifest="build/MANIFEST.MF" />
	</target>

	<target name="scaladocs" depends="init">
		<mkdir dir="build/api" />
		<scaladoc destdir="build/api">
			<classpath>
				<fileset dir="lib">
					<include name="**/*.jar" />
				</fileset>
				<pathelement location="${scala-lib.jar}" />
			</classpath>
			<src path="src/main/scala" />
			<include name="**/*.scala" />
		</scaladoc>
	</target>
</project>
